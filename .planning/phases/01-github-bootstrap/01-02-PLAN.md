---
phase: 01-github-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - README.md
  - docs/bootstrap-inventory.md
autonomous: true
requirements:
  - BOOT-01
  - BOOT-02
user_setup: []
must_haves:
  truths:
    - "Новый maintainer может пройти путь от git clone до первого рабочего scan без скрытых шагов"
    - "Quickstart явно отделяет обязательный base scan-path от Telegram-only setup"
    - "Inventory описывает обязательные properties, листы, advanced services и installable triggers на русском"
  artifacts:
    - path: "README.md"
      provides: "Главный линейный quickstart"
      contains: "ROOT_FOLDER_ID"
    - path: "docs/bootstrap-inventory.md"
      provides: "Операционный inventory bootstrap-зависимостей"
      contains: "Script Properties"
  key_links:
    - from: "README.md"
      to: ".clasp.example.json"
      via: "local binding setup steps"
      pattern: ".clasp.example.json"
    - from: "README.md"
      to: "00_MainUi.gs"
      via: "first-run flow referencing menu actions"
      pattern: "uiSetRootFolder|scanAndUpdate"
    - from: "README.md"
      to: "docs/bootstrap-inventory.md"
      via: "explicit quickstart handoff to full inventory"
      pattern: "bootstrap-inventory"
    - from: "docs/bootstrap-inventory.md"
      to: "appsscript.json"
      via: "manifest dependency mapping"
      pattern: "Drive"
---

<objective>
Описать русский bootstrap happy path и вынести в отдельный inventory все обязательные и условные инфраструктурные зависимости проекта.

Purpose: Закрыть BOOT-01 и BOOT-02 пользовательским артефактом, по которому другой maintainer реально сможет поднять container-bound окружение без обращения к автору.
Output: Главный `README.md` с линейным quickstart и `docs/bootstrap-inventory.md` с таблицами properties/sheets/services/triggers.
</objective>

<execution_context>
@C:/Users/Mikhail/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Mikhail/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-github-bootstrap/01-CONTEXT.md
@.planning/phases/01-github-bootstrap/01-RESEARCH.md
@.clasp.example.json
@appsscript.json
@00_MainUi.gs
@10_DriveRepo.gs
@11_SheetsRepo.gs
@20_View.gs
@31_Telegram.gs
@40_ScanJob.gs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Собрать главный русский quickstart от clone до первого scan</name>
  <files>README.md</files>
  <action>Создай основной bootstrap-документ на русском языке. Он должен вести по одному стабильному happy path: `git clone` -> новый Spreadsheet -> новый Apps Script container -> локальная `clasp`-привязка через `.clasp.example.json` -> `clasp push/pull` -> открытие таблицы -> запуск menu entry points `uiSetRootFolder()` и `scanAndUpdate()` -> признаки успешного first-run. Не документируй Telegram setup как обязательный шаг; вынеси его в отдельный optional block. Обязательно объясни, почему проект именно container-bound и почему этот quickstart не начинается со standalone script. В конце quickstart добавь явный handoff на `docs/bootstrap-inventory.md`, чтобы maintainer после happy path видел полный inventory обязательных и условных зависимостей.</action>
  <verify>
    <automated>Get-Content README.md | Select-String 'ROOT_FOLDER_ID|scanAndUpdate|.clasp.example.json|Spreadsheet|Apps Script|bootstrap-inventory' | Out-Null</automated>
  </verify>
  <done>В корне репозитория есть один главный quickstart, по которому новый maintainer может пройти базовый bootstrap без скрытых ручных решений.</done>
</task>

<task type="auto">
  <name>Task 2: Вынести bootstrap inventory по properties, sheets, services и triggers</name>
  <files>docs/bootstrap-inventory.md</files>
  <action>Создай отдельный русский inventory-документ с таблицами и пометками `обязательно` / `условно` / `опционально`. Зафиксируй реальные сущности из кода: `ROOT_FOLDER_ID`, служебные `STOP_SCAN` и `SYNC_CURSOR`, Telegram keys, листы `Разрешения`, `Logs`, `Витрина`, `Контакты`, advanced `Drive` service из `appsscript.json`, installable triggers `continueScan` и `tgProcessEmployeeBotUpdates`. Для каждого пункта укажи, кто его создаёт/использует и как maintainer проверяет его наличие. Отдельно пометь, что `ImagesService` используется кодом QR flow и требует явной проверки в checklist, потому что в tracked manifest его происхождение неочевидно.</action>
  <verify>
    <automated>Get-Content docs/bootstrap-inventory.md | Select-String 'ROOT_FOLDER_ID|Разрешения|Витрина|Drive|continueScan|tgProcessEmployeeBotUpdates|ImagesService' | Out-Null</automated>
  </verify>
  <done>Bootstrap inventory документирует все обязательные и условные зависимости этого репозитория на русском языке и не смешивает base scan-path с Telegram-only setup.</done>
</task>

</tasks>

<verification>
1. `README.md` можно читать как единственный вход в bootstrap.
2. `docs/bootstrap-inventory.md` перечисляет все реальные свойства, листы, service dependencies и installable triggers из кода.
3. Quickstart и inventory согласованы: README ссылается на inventory, а inventory не требует ручных действий, которых нет в quickstart.
</verification>

<success_criteria>
- В корне репозитория появляется линейный quickstart на русском языке.
- Отдельный inventory фиксирует обязательные/условные bootstrap-зависимости.
- Maintainer может понять, что нужно для первого scan, а что нужно только для Telegram сценариев.
</success_criteria>

<output>
After completion, create `.planning/phases/01-github-bootstrap/01-02-SUMMARY.md`
</output>
