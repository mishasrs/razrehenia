---
phase: 01-github-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .clasp.example.json
  - .claspignore
  - docs/repository-sanitization.md
  - scripts/check-public-surface.ps1
autonomous: true
requirements:
  - BOOT-01
  - BOOT-03
user_setup: []
must_haves:
  truths:
    - "Tracked surface репозитория не содержит live script bindings и production ids"
    - "Maintainer может отличить tracked template от локальной operational binding-конфигурации"
    - "Из корня репозитория запускается повторяемая automated-проверка public-ready surface"
  artifacts:
    - path: ".clasp.example.json"
      provides: "Обезличенный clasp template с явными placeholders"
      contains: "YOUR_SCRIPT_ID"
    - path: ".gitignore"
      provides: "Правило исключения локального .clasp.json и других non-public artifacts"
      contains: ".clasp.json"
    - path: "docs/repository-sanitization.md"
      provides: "Постоянная sanitization policy для публичного репозитория"
      contains: "no live bindings in Git"
    - path: "scripts/check-public-surface.ps1"
      provides: "Автоматизированный audit public surface"
      contains: "git ls-files"
  key_links:
    - from: ".gitignore"
      to: ".clasp.json"
      via: "ignore rule"
      pattern: "\\.clasp\\.json"
    - from: "scripts/check-public-surface.ps1"
      to: ".clasp.example.json"
      via: "placeholder and leak scan"
      pattern: "YOUR_SCRIPT_ID|YOUR_SPREADSHEET_ID"
    - from: "docs/repository-sanitization.md"
      to: "scripts/check-public-surface.ps1"
      via: "documented audit command"
      pattern: "check-public-surface"
---

<objective>
Закрепить публичную sanitization-поверхность репозитория и автоматизировать базовую проверку на утечки live bindings.

Purpose: Закрыть техническую сторону BOOT-01/BOOT-03 до написания quickstart, чтобы дальнейшая документация ссылалась уже на безопасные tracked artifacts.
Output: Нормализованные ignore/template файлы, документ с sanitization policy и PowerShell-скрипт для audit public surface.
</objective>

<execution_context>
@C:/Users/Mikhail/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Mikhail/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-github-bootstrap/01-CONTEXT.md
@.planning/phases/01-github-bootstrap/01-RESEARCH.md
@.clasp.example.json
@.claspignore
@.gitignore
@appsscript.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Нормализовать tracked config surface для публичного репозитория</name>
  <files>.gitignore, .clasp.example.json, .claspignore</files>
  <action>Проверь текущие tracked конфигурационные файлы и закрепи правило `tracked template / local binding only`. Оставь в `.clasp.example.json` только явные placeholders вроде `YOUR_SCRIPT_ID` и `YOUR_SPREADSHEET_ID`, не добавляй в tracked surface никакие live ids или реалистично-похожие operational bindings. Убедись, что `.gitignore` явно исключает рабочий `.clasp.json`, а `.claspignore` остаётся совместимым с текущим Apps Script push surface без случайной публикации docs/scripts в контейнер.</action>
  <verify>
    <automated>git check-ignore .clasp.json *> $null; if (-not $?) { throw '.clasp.json must be ignored' }; if (git ls-files | Select-String '^[.]clasp[.]json$') { throw '.clasp.json must not be tracked' }; Get-Content .clasp.example.json | Select-String 'YOUR_SCRIPT_ID|YOUR_SPREADSHEET_ID' | Out-Null</automated>
  </verify>
  <done>Tracked config surface показывает только redacted template и не допускает попадания локального `.clasp.json` в Git.</done>
</task>

<task type="auto">
  <name>Task 2: Зафиксировать постоянную sanitization policy рядом с кодом</name>
  <files>docs/repository-sanitization.md</files>
  <action>Создай короткий русский документ, который фиксирует, какие артефакты можно хранить в Git, какие считаются operational data и как использовать placeholders в конфигурации и docs. Привяжи policy к реальным файлам репозитория: `.clasp.example.json`, `.claspignore`, `appsscript.json`, будущему `README.md`. Не пиши абстрактные рассуждения про security; документ должен быть привязан к этому репозиторию и его tracked surface.</action>
  <verify>
    <automated>Get-Content docs/repository-sanitization.md | Select-String 'no live bindings in Git|.clasp.example.json|appsscript.json' | Out-Null</automated>
  </verify>
  <done>В репозитории есть явная policy, объясняющая maintainer, где проходит граница между public artifacts и локальной operational-конфигурацией.</done>
</task>

<task type="auto">
  <name>Task 3: Добавить automated audit public surface</name>
  <files>scripts/check-public-surface.ps1</files>
  <action>Создай PowerShell-скрипт, который из корня репозитория проверяет tracked files и падает на типовых нарушениях BOOT-03: появление `.clasp.json` в tracked set, placeholder drift в `.clasp.example.json`, явные live-binding паттерны в tracked docs/config и другие очевидные утечки operational ids. Скрипт должен быть детерминированным, без внешних зависимостей, и пригодным для ручного запуска новым maintainer. Свяжи его с policy-документом из Task 2 через описанный audit command.</action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/check-public-surface.ps1</automated>
  </verify>
  <done>Из корня репозитория выполняется одна команда, которая подтверждает public-ready surface или даёт понятную ошибку при нарушении sanitization policy.</done>
</task>

</tasks>

<verification>
1. `git ls-files` не содержит `.clasp.json`.
2. `.clasp.example.json` и связанная policy согласованы между собой по placeholder-модели.
3. Скрипт public-surface audit запускается локально без ручных зависимостей и проверяет tracked artifacts, а не только код.
</verification>

<success_criteria>
- Репозиторий закрепляет безопасный tracked/untracked split для Apps Script binding.
- В docs есть короткая, предметная sanitization policy с привязкой к реальным файлам.
- Public-ready audit можно запустить автоматизированно из корня репозитория.
</success_criteria>

<output>
After completion, create `.planning/phases/01-github-bootstrap/01-01-SUMMARY.md`
</output>
